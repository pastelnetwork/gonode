package fake

import "github.com/pastelnetwork/gonode/tools/pastel-api/api/services/fake/models"

func newFullListMasterNodes(nodes models.FullListMasterNodes) models.FullListMasterNodes {
	return nodes
}
